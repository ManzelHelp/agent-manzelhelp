"use client";

// This page reuses the job details page component
// The component will automatically detect it's on the /edit route
// via usePathname() and activate edit mode
import JobDetailPage from "../page";

export default JobDetailPage;
